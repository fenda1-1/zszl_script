# 1.20.1 迁移计划 ToDo

## 对我自己的约束规范

- 只能做“完整迁移 + 定向删除”，不做简化版、占位版、空实现版。
- 删除范围必须可证明，只能删除明确属于旧版隐藏服务器内容的模块；其余功能必须保留并迁移。
- 非删除目标的功能，不允许因为 1.20.1 API 变化就静默砍掉、降级或改成临时原版界面。
- 所有删除都必须同步覆盖 Java、JSON、语言文件、GUI 分类、配置、分享码、导入导出、可见性控制、按键、命令、资源路径、数据持久化与存档兼容点。
- 最终验证不能只看编译通过；必须保留“功能迁移覆盖矩阵”，逐项写清旧功能、1.20.1 对应实现、验证方式、完成状态。
- 如果存在阻塞，必须明确列出阻塞点、受影响功能、替代方案和未完成原因，不能假装完成。

## 项目要求与当前边界

- 迁移目标固定为 `Minecraft Forge 1.20.1`。
- 迁移前先做旧版清理，再做 1.20.1 迁移设计。
- 当前仓库内硬约束与本次口头要求存在冲突：
  - 仓库约束要求“只能删除再生之路及其直接/间接相关内容”。
  - 当前口头目标还要求删除“魔塔之巅”。
  - 因此本轮已先按硬约束完成“再生之路”定向删除与编译验证；`魔塔之巅` 迁移前删除被列为显式阻塞项，不能在未解除约束冲突前直接执行。

## 当前阶段结果

- 日期：`2026-04-13`
- 已执行编译验证：`./gradlew compileJava`
- 结果：`BUILD SUCCESSFUL`
- 已完成的预迁移清理：
  - 从 `src/main/resources/pathsequences.json` 删除全部 `再生之路` 内置路径，仅保留 `魔塔之巅` 4 条旧路径。
  - 主界面不再创建 `再生之路` 分类。
  - `击杀计时` 保留，并迁出 `再生之路` 分类到通用区。
  - 隐藏服务器配置默认只保留 `魔塔之巅` 规则。
  - 抓包规则内置分组从“再生之路”调整为“内置规则”，避免保留下来的通用抓包能力继续挂在已删除服务器分组下。
  - 关闭 `自动技能`、`签到/在线` 等 `再生之路` 运行时入口，不再在全局 Tick 中执行。
  - 停止注册 `快速兑换`、`冒险经验面板` 运行时入口。
  - 已删除 `极限攻速` 与 `潜影盒回弹修复` 的事件注册、按键动作、GUI 入口与实现文件。
  - 分享码已移除 `再生之路` 相关配置文件注册项：`auto_skills_v2.json`、`auto_signin_online_config.json`、`ad_exp_panel_config.json`、`quick_exchange_config.json`、`auto_stacking_config.json`、`keycommand_leaveconfig.json`。

## 删除收尾清单

- [ ] 二次全文追踪 `再生之路` 文本引用，继续清理未使用的文案、死代码文件和旧 GUI 分支。
- [ ] 明确 `魔塔之巅` 删除边界与授权冲突，确认是否解除“只能删再生之路”的仓库约束。
- [ ] 若约束解除，删除 `魔塔之巅` 的路径、过滤、语言、可见性规则、配置和分享码联动点。
- [ ] 对旧配置目录兼容策略做决定：
  - `config/再生之路脚本`
  - 当前保留是为了兼容旧版配置迁移，不代表保留服务器功能名称。

## Forge 1.20.1 迁移主计划

### 阶段 A：基线与工程骨架

- [ ] 新建 `1.20.1` 迁移分支或目录策略，明确不覆盖当前 1.12.2 可回退基线。
- [ ] 将构建系统切换到 Forge `1.20.1` 对应 ForgeGradle、Java 17、映射与运行配置。
- [ ] 梳理 `coremod / mixin / shadowbaritone / launch hook` 在 1.20.1 下的替换方案。
- [ ] 建立 `client bootstrap`、配置目录迁移、资源加载、命令注册、按键注册的 1.20.1 骨架。

### 阶段 B：底层兼容层

- [ ] 事件总线迁移：Tick、GUI、聊天、世界、实体、背包、网络、按键。
- [ ] 网络层迁移：旧 `SPacket/CPacket/FMLProxyPacket` 到 1.20.1 `Clientbound/Serverbound/CustomPayload` 适配。
- [ ] 渲染层迁移：HUD、Overlay、字体、鼠标、屏幕缩放、矩形绘制、主题绘制。
- [ ] 输入层迁移：键鼠输入、鼠标脱离、模拟输入、AutoHotKey 外部输入联动。
- [ ] 数据层迁移：配置读写、Profile 目录、分享码、导入导出、旧配置升级脚本。

### 阶段 C：系统级模块迁移

- [ ] GUI 主界面与所有二级/三级配置页完整迁移。
- [ ] 路径序列系统迁移：动作、步骤、分类、收藏、最近使用、失败处理、执行日志。
- [ ] 节点图系统迁移：图模型、编辑器、模板库、草稿、审计日志、热键。
- [ ] 变量系统迁移：全局/序列/局部/临时变量与捕获数据联动。
- [ ] 抓包/发包/字段规则/拦截规则/捕获规则迁移。
- [ ] ShadowBaritone 集成迁移或适配层重构。
- [ ] Other Features 六大类模块迁移：`movement / block / item / render / world / misc`。

### 阶段 D：逐模块验收

- [ ] 配置兼容验证：旧配置导入、目录迁移、默认值兼容。
- [ ] GUI 一致性验证：布局、分页、交互、拖拽、搜索、滚动、右键菜单。
- [ ] 行为一致性验证：开关语义、运行时状态、断线重连、Profile 切换。
- [ ] 数据一致性验证：分享码、导入导出、分类显示、可见性控制。
- [ ] 回归测试：非 `再生之路` 功能在 1.20.1 下逐项验收。

## 1.20.1 实施顺序

1. 完成删除收尾，冻结 1.12.2 清理基线。
2. 搭建 Forge 1.20.1 工程骨架并确认能启动空模组。
3. 迁移配置系统、Profile 系统、分享码系统。
4. 迁移 GUI 基础组件与主题系统。
5. 迁移路径序列系统与动作解析器。
6. 迁移节点图与变量系统。
7. 迁移抓包/发包/字段规则/拦截规则。
8. 迁移 Other Features 与 ShadowBaritone 集成。
9. 做全量功能回归和旧配置兼容验证。

## 功能迁移覆盖矩阵

| 旧功能/模块 | 当前处理 | 1.20.1 对应实现 | 验证方式 | 当前状态 |
| --- | --- | --- | --- | --- |
| 主界面 Overlay 与主题组件 | 保留并计划完整迁移 | 重建 `Screen + GuiGraphics + 自定义主题渲染层` | 页面打开、分页、滚动、拖拽验证 | 待迁移 |
| 公共功能分类 | 保留 | 1.20.1 下复刻当前布局与命令绑定 | UI 回归 | 待迁移 |
| `再生之路` 分类 | 已删除 | 不迁移 | 主界面分类检查、全文搜索 | 已完成 |
| `魔塔之巅` 分类 | 暂保留 | 待确认是否允许删除 | 约束解除后执行 | 阻塞 |
| 内置路径 `再生之路` 序列 | 已删除 | 不迁移 | `pathsequences.json` 检查 | 已完成 |
| 内置路径 `魔塔之巅` 序列 | 保留待决 | 若允许删除则不迁移，否则迁移或下线方案待定 | 路径清单核对 | 阻塞 |
| 路径序列编辑器 | 保留并完整迁移 | 1.20.1 `Screen` 重构 + 数据兼容层 | 编辑/保存/执行回归 | 待迁移 |
| 动作系统 | 保留并完整迁移 | 动作注册表 + 参数 schema 迁移 | 动作编辑器与执行器联测 | 待迁移 |
| 节点图系统 | 保留并完整迁移 | 节点编辑器/运行器/模板库迁移 | 图编辑/执行/导入导出 | 待迁移 |
| 变量系统 | 保留并完整迁移 | 作用域变量与表达式适配 | 条件分支回归 | 待迁移 |
| 抓包规则系统 | 保留并完整迁移 | 1.20.1 网络拦截与解码适配 | 收包/发包/规则命中验证 | 待迁移 |
| 抓包规则分组“再生之路” | 已改为“内置规则” | 不再保留旧服务器分组名 | GUI 分组检查 | 已完成 |
| 隐藏服务器功能配置 | 缩减为仅 `魔塔之巅` | 1.20.1 继续做可见性规则管理 | 配置页与保存验证 | 部分完成 |
| 自动技能 | 视为 `再生之路` 相关，已停运行时入口 | 不迁移 | 全局 Tick 与入口检查 | 已完成 |
| 签到/在线后台 | 视为 `再生之路` 相关，已停运行时入口 | 不迁移 | 全局 Tick 与入口检查 | 已完成 |
| 快速兑换 | 视为 `再生之路` 相关，已停运行时入口 | 不迁移 | 事件注册与分享码检查 | 已完成 |
| 冒险经验面板 | 视为 `再生之路` 相关，已停运行时入口 | 不迁移 | 事件注册与分享码检查 | 已完成 |
| 死亡自动重进 | 绑定 `再生之路` 旧副本序列，待做彻底删链 | 不迁移 | 全文引用与配置清理 | 待补清理 |
| 自动叠加潜影盒 | 依赖旧包会话，待做彻底删链 | 不迁移 | 全文引用与配置清理 | 待补清理 |
| 极限攻速 | 视为 `再生之路` 功能，已删除 | 不迁移 | 代码入口、按键、GUI、实现文件检查 | 已完成 |
| 击杀计时 | 唯一保留并迁到通用区的原 `再生之路` 功能 | 1.20.1 保留 HUD 与配置 | HUD 渲染与配置回归 | 待迁移 |
| 潜影盒回弹修复 | 视为 `再生之路` 功能，已删除 | 不迁移 | 代码入口、GUI、实现文件检查 | 已完成 |
| 聊天优化/智能复制 | 保留并去掉“仅再生之路”文案 | 1.20.1 迁移聊天组件处理链 | 聊天点击与复制验证 | 待迁移 |
| Keybind 管理 | 保留并完整迁移 | 1.20.1 KeyMapping 适配 | 按键绑定回归 | 待迁移 |
| Profile/分享码系统 | 保留并完整迁移 | 数据迁移脚本 + 1.20.1 UI | 导入导出/分享码回归 | 待迁移 |
| Other Features 六大类 | 保留并完整迁移 | 逐类事件与渲染适配 | 分类逐项验收 | 待迁移 |
| ShadowBaritone 集成 | 保留并完整迁移 | 兼容层或重构 | 导航/指令/路径回归 | 待迁移 |

## 当前阻塞项

- `魔塔之巅` 删除授权冲突：
  - 影响功能：隐藏服务器功能、内置路径、分类过滤、迁移前清理范围。
  - 当前处理：暂不删除，避免违反仓库硬约束。
  - 替代方案：明确解除“只能删再生之路”的约束后，再做 `魔塔之巅` 专项删除。
- `再生之路` 仍有少量遗留源码/文案引用未完全摘除：
  - 影响功能：源码洁净度、死代码清理完整性。
  - 当前处理：本轮先完成运行时入口下线与编译通过，下一轮按全文追踪继续收尾。
